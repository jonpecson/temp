{
  "routes": [
    {
      "method": "GET",
      "path": "/scanners",
      "handler": "scanner.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/scanners/count",
      "handler": "scanner.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/scanners/:id",
      "handler": "scanner.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/scanners",
      "handler": "scanner.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/scanners/:id",
      "handler": "scanner.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/scanners/:id",
      "handler": "scanner.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
